import moment from 'moment';

function solution() { // Пример использования moment
    const now = moment();
    return now.format();
}

module.exports = {
    solution  // Экспорт функции solution 
};